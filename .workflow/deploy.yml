name: Deploy with Self-Hosted Runner (Windows)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, windows]
    steps:
      - name: Checkout code
        run: | 
            node --version 
            npm --version
